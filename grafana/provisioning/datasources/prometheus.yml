apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # URL interne Docker - utilise le nom du service et le réseau Docker
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      httpMethod: POST
      timeInterval: 15s
